#include "main.h"

/**
 * _isdigit - searchs for a number (0 through 9)
 * @c: number to be checked
 * Return: 1 if c is a number, 0 ifnot
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

